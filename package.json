{
  "name": "imagenew-worker",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "format": "prettier -w src public"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "prettier": "^3.2.5",
    "typescript": "^5.4.0",
    "wrangler": "^3.88.0"
  }
}
